language: python
python:
  - "3.6"
  - "3.7-dev"
install:
  - pip install codecov
  - pip install -r requirements.txt

script:
  - coverage run -pm unittest discover
  - coverage combine

after_success:
  - codecov